# Optional: python packages for running the *jobs* on your laptop (outside Docker).
# Inside compose everything is already baked into the images, so you do not need
# this file to run the project - only to hack on it with an IDE / debugger.
pyspark==3.5.1
pandas>=2.0,<2.3
pyarrow>=14
redis>=5.0
psycopg2-binary>=2.9
confluent-kafka>=2.3,<3
requests>=2.31
